/**
 * Shared surface for the marketing cards. The landing page forces pure black
 * regardless of the app theme, so these cards cannot lean on `bg-card`.
 */
export const LANDING_CARD =
	"relative overflow-hidden rounded-xl border border-white/10 bg-white/[0.02] text-white shadow-[inset_0_1px_0_rgba(255,255,255,0.06)] ring-0 backdrop-blur-sm";
